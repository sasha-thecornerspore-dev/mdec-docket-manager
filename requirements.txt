# Core — required to run the app
fastapi>=0.111
uvicorn[standard]>=0.30
playwright>=1.45
keyring>=25.0
imap-tools>=1.7
anthropic>=0.34
pdfplumber>=0.11
requests>=2.32

# Optional features — app runs without these; the feature explains what's missing
# OCR pipeline (also needs Tesseract + Ghostscript installed on the system):
ocrmypdf>=16.0
